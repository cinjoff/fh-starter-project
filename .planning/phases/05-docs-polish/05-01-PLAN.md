---
phase: 05-docs-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/app/robots.ts
  - src/app/sitemap.ts
  - src/proxy.ts
  - src/lib/env.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/proxy.ts
  - src/lib/actions/types.ts
  - src/app/error.tsx
  - src/app/global-error.tsx
autonomous: true

must_haves:
  truths:
    - "A developer cloning the repo sees a comprehensive README with setup instructions, feature list, architecture, commands, env configuration, and deployment notes"
    - "app/robots.ts returns a valid robots.txt response and app/sitemap.ts returns a valid sitemap XML"
    - "Non-obvious source files have concise header comments explaining their purpose and usage"
    - "pnpm build and pnpm test pass green"
  artifacts:
    - path: "README.md"
      provides: "comprehensive project documentation for template adopters"
      contains: "## Getting Started"
    - path: "src/app/robots.ts"
      provides: "dynamic robots.txt via Next.js metadata API"
      contains: "export default function robots"
    - path: "src/app/sitemap.ts"
      provides: "dynamic sitemap via Next.js metadata API"
      contains: "export default function sitemap"
  key_links:
    - from: "README.md"
      to: "CLAUDE.md"
      via: "README references CLAUDE.md for LLM/agent conventions"
    - from: "src/app/robots.ts"
      to: "src/app/sitemap.ts"
      via: "robots.txt references sitemap URL"

requirements:
  - REQ-10
  - REQ-10a
---

<objective>Complete Phase 5: Documentation and Polish. Rewrite README for template adopters, add robots.ts + sitemap.ts route handlers, and add self-documenting header comments to non-obvious source files. Final build + test verification.</objective>

<context>
@README.md
@CLAUDE.md
@src/proxy.ts
@src/lib/env.ts
@src/lib/supabase/client.ts
@src/lib/supabase/server.ts
@src/lib/supabase/proxy.ts
@src/lib/actions/types.ts
@src/app/error.tsx
@src/app/global-error.tsx
@src/app/layout.tsx
@.planning/phases/05-docs-polish/05-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: README rewrite + robots.ts + sitemap.ts</name>
  <files>README.md, src/app/robots.ts, src/app/sitemap.ts</files>
  <action>
    1. Rewrite README.md with these sections:
       - Project name + one-line description
       - Features list (auth, DB, error tracking, testing, CI, security headers, env validation, release automation)
       - Tech Stack (Next.js 16, React 19, TypeScript, Tailwind v4, Shadcn/ui, Phosphor Icons, Supabase, Sentry, Zod, Biome, pnpm)
       - Getting Started (prerequisites: Node 20+, pnpm; clone, install, configure .env, run dev)
       - Environment Variables table (all vars from env.ts with descriptions and defaults)
       - Commands table (dev, build, test, test:e2e, check, typecheck, format)
       - Project Structure (src/app, src/lib, src/components, e2e, supabase)
       - Deployment section (Vercel preset, env vars to set)
       - Extending section (adding pages, API routes, DB migrations, new shadcn components)
       - Reference to CLAUDE.md for LLM/agent conventions
    2. Create src/app/robots.ts using Next.js Metadata API (export default function robots(): MetadataRoute.Robots)
       - Allow all user agents, disallow /api/
       - Include sitemap URL using NEXT_PUBLIC_APP_URL env var with fallback
    3. Create src/app/sitemap.ts using Next.js Metadata API (export default function sitemap(): MetadataRoute.Sitemap)
       - Include root URL with lastModified date
       - Use NEXT_PUBLIC_APP_URL env var with fallback
  </action>
  <verify>
    - README has all required sections (grep for ## headings)
    - robots.ts exports default function
    - sitemap.ts exports default function
    - pnpm typecheck passes
  </verify>
  <done>Developer cloning the repo sees comprehensive README. robots.ts and sitemap.ts return valid responses.</done>
</task>

<task type="auto">
  <name>Task 2: Self-documenting header comments</name>
  <files>src/proxy.ts, src/lib/env.ts, src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/proxy.ts, src/lib/actions/types.ts, src/app/error.tsx, src/app/global-error.tsx</files>
  <action>
    Add concise 1-3 line JSDoc-style header comments to each file explaining purpose and usage context:
    1. src/proxy.ts — Next.js 16 proxy (replaces middleware.ts): session refresh + security headers
    2. src/lib/env.ts — Type-safe environment variables via t3-env with Zod validation
    3. src/lib/supabase/client.ts — Browser-side Supabase client for client components
    4. src/lib/supabase/server.ts — Server-side Supabase client with cookie handling for RSC/route handlers
    5. src/lib/supabase/proxy.ts — Supabase session refresh called from proxy.ts on every request
    6. src/lib/actions/types.ts — Shared return type for server actions (success/error + typed data)
    7. src/app/error.tsx — Route-level error boundary with Sentry reporting and retry
    8. src/app/global-error.tsx — Root layout crash handler (last-resort error boundary)

    Format: /** one-line description */ or 2-3 line block. No @param/@returns for simple files.
  </action>
  <verify>
    - Each target file starts with a /** comment (after any "use client" directive)
    - pnpm typecheck passes
    - pnpm check passes (Biome)
  </verify>
  <done>Non-obvious source files have concise header comments. Code is self-documenting for humans and LLMs.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification</name>
  <files>README.md</files>
  <action>
    1. Run pnpm build — must succeed
    2. Run pnpm test — all tests must pass
    3. Run pnpm check — Biome lint/format clean
    4. Run pnpm typecheck — no type errors
    5. Verify CLAUDE.md is under 40 lines of meaningful content (excluding blank lines)
  </action>
  <verify>
    - All four commands exit 0
    - No regressions from Phase 4
  </verify>
  <done>pnpm build and pnpm test pass green. Template is ready for use.</done>
</task>
</tasks>

<verification>
pnpm typecheck && pnpm check && pnpm test && pnpm build
</verification>

<success_criteria>
- Developer cloning the repo sees a comprehensive README with setup instructions, feature list, architecture, commands, env configuration, and deployment notes
- app/robots.ts returns a valid robots.txt response and app/sitemap.ts returns a valid sitemap XML
- Non-obvious source files have concise header comments explaining their purpose and usage
- pnpm build and pnpm test pass green
</success_criteria>

<output>.planning/phases/05-docs-polish/05-01-SUMMARY.md</output>
