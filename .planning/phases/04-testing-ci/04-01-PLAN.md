---
phase: 04-testing-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/tests/dates.test.ts
  - src/tests/submit-button.test.tsx
  - src/tests/setup.ts
  - playwright.config.ts
  - e2e/home.spec.ts
  - .github/workflows/ci.yml
  - release-please-config.json
  - .release-please-manifest.json
  - .github/workflows/release.yml
autonomous: true

must_haves:
  truths:
    - "pnpm test runs Vitest and all tests pass including dates utility and SubmitButton component"
    - "pnpm test:e2e runs Playwright against dev server and home page + 404 tests pass"
    - "GitHub Actions ci.yml runs lint, typecheck, and vitest on push to main and PRs"
    - "release-please config exists with node type and conventional-commits plugin"
  artifacts:
    - path: vitest.config.ts
      provides: Vitest configuration with jsdom and coverage
      contains: jsdom
    - path: src/tests/dates.test.ts
      provides: unit tests for date utilities
      contains: formatDate
    - path: src/tests/submit-button.test.tsx
      provides: component test for SubmitButton
      contains: SubmitButton
    - path: playwright.config.ts
      provides: Playwright configuration with webServer
      contains: webServer
    - path: e2e/home.spec.ts
      provides: E2E tests for home page and 404
      contains: page.goto
    - path: .github/workflows/ci.yml
      provides: CI pipeline
      contains: pnpm test
    - path: release-please-config.json
      provides: release-please configuration
      contains: node
    - path: .github/workflows/release.yml
      provides: release-please GitHub Action
      contains: release-please-action
  key_links:
    - from: vitest.config.ts
      to: src/tests/setup.ts
      via: setupFiles reference
    - from: .github/workflows/ci.yml
      to: vitest.config.ts
      via: "pnpm test command uses vitest config"
    - from: playwright.config.ts
      to: e2e/home.spec.ts
      via: testDir configuration

requirements:
  - REQ-06
  - REQ-08
  - REQ-08a
---

<objective>
Set up Vitest with example tests, Playwright with E2E smoke tests, GitHub Actions CI, and release-please automation.
</objective>

<context>
@src/lib/dates.ts
@src/components/submit-button.tsx
@src/app/page.tsx
@src/app/not-found.tsx
@package.json
@tsconfig.json
@.planning/phases/04-testing-ci/04-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Vitest config + unit and component tests</name>
  <files>vitest.config.ts, src/tests/setup.ts, src/tests/dates.test.ts, src/tests/submit-button.test.tsx</files>
  <action>
  1. Install dev deps: `pnpm add -D @vitejs/plugin-react @testing-library/react @testing-library/jest-dom @vitest/coverage-v8 vite-tsconfig-paths jsdom`
  2. Create `vitest.config.ts`:
     - Import `defineConfig` from `vitest/config` and `react` from `@vitejs/plugin-react`
     - Use `vite-tsconfig-paths` plugin (already installed) for `@/` path aliases
     - Set test environment to `jsdom`, setupFiles to `./src/tests/setup.ts`
     - Configure coverage provider `v8` with reporter `['text', 'lcov']`
     - Set `include` to `['src/**/*.test.{ts,tsx}']`
  3. Create `src/tests/setup.ts`:
     - Import `@testing-library/jest-dom/vitest` for matcher extensions
  4. Create `src/tests/dates.test.ts`:
     - Test `formatDate` with a known date string → expected "MMM d, yyyy" output
     - Test `formatRelative` with a date 2 hours ago → contains "hours ago"
     - Test `formatDate` with a Date object (not just string)
     - Test edge: invalid input handling if applicable
  5. Create `src/tests/submit-button.test.tsx`:
     - Render SubmitButton with children text, verify it renders a button
     - Verify it passes through className and other props
     - Test that it renders with default text when not pending
  6. Run `pnpm test` — all tests must pass.
  </action>
  <verify>Run `pnpm test` and confirm all tests pass. Run `pnpm typecheck` to ensure no type errors.</verify>
  <done>Vitest configured with jsdom + coverage. dates.test.ts and submit-button.test.tsx pass. `pnpm test` exits green.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright config + E2E smoke tests</name>
  <files>playwright.config.ts, e2e/home.spec.ts</files>
  <action>
  1. Run `npx playwright install chromium` to install browser binary.
  2. Create `playwright.config.ts`:
     - Import `defineConfig` from `@playwright/test`
     - Set `testDir: './e2e'`, `fullyParallel: true`, `forbidOnly: !!process.env.CI`
     - Set `retries: process.env.CI ? 2 : 0`
     - Configure single project: `chromium`
     - Add `webServer`: command `pnpm dev`, url `http://localhost:3000`, reuseExistingServer `!process.env.CI`
     - Set `use.baseURL` to `http://localhost:3000`
  3. Create `e2e/home.spec.ts`:
     - Test "home page loads": navigate to `/`, expect page title or heading to contain expected text
     - Test "404 page shows for unknown routes": navigate to `/this-does-not-exist`, expect "404" text visible and "Go home" link present
  4. Run `pnpm test:e2e` — both tests must pass.
  </action>
  <verify>Run `pnpm test:e2e` and confirm both tests pass.</verify>
  <done>Playwright configured with webServer. home.spec.ts tests home page load and 404 page. `pnpm test:e2e` exits green.</done>
</task>

<task type="auto">
  <name>Task 3: GitHub Actions CI + release-please</name>
  <files>.github/workflows/ci.yml, release-please-config.json, .release-please-manifest.json</files>
  <action>
  1. Create `.github/workflows/ci.yml`:
     - Name: CI
     - Triggers: push to main, pull_request
     - Single job `ci` on `ubuntu-latest`
     - Steps: checkout, setup Node 22, setup pnpm (use `pnpm/action-setup`), install deps (`pnpm install --frozen-lockfile`), lint (`pnpm check`), typecheck (`pnpm typecheck`), test (`pnpm test`)
     - Do NOT include Playwright (deferred)
  2. Create `.github/workflows/release.yml`:
     - Name: Release
     - Triggers: push to main only
     - Uses `googleapis/release-please-action@v4` with `config-file` and `manifest-file`
  3. Create `release-please-config.json`:
     - `packages: { ".": { "release-type": "node", "changelog-sections": [{"type":"feat","section":"Features"},{"type":"fix","section":"Bug Fixes"},{"type":"docs","section":"Documentation"},{"type":"refactor","section":"Refactoring"},{"type":"test","section":"Tests"},{"type":"chore","section":"Miscellaneous"}] } }`
  4. Create `.release-please-manifest.json`:
     - `{ ".": "0.1.0" }` (initial version matching package.json)
  5. Verify YAML syntax: run `cat .github/workflows/ci.yml | head -1` (basic sanity).
  </action>
  <verify>Verify files exist and YAML is valid. Run `pnpm check` and `pnpm typecheck` as final validation.</verify>
  <done>ci.yml runs lint+typecheck+test on push/PR. release.yml uses release-please. Config files present with node type.</done>
</task>
</tasks>

<verification>
pnpm test
pnpm test:e2e
pnpm typecheck
pnpm check
</verification>

<success_criteria>
- pnpm test runs Vitest and all tests pass including dates utility and SubmitButton component
- pnpm test:e2e runs Playwright against dev server and home page + 404 tests pass
- GitHub Actions ci.yml runs lint, typecheck, and vitest on push to main and PRs
- release-please config exists with node type and conventional-commits plugin
</success_criteria>

<output>.planning/phases/04-testing-ci/04-01-SUMMARY.md</output>
