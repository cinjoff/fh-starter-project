# Login Page

- Email/password form with Zod validation
- Google OAuth button auto-appears when `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` are set
- Handles OAuth error callbacks (`?error=access_denied` etc.) with user-facing messages
